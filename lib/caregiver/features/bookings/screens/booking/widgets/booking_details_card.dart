import 'package:flutter/material.dart';

class CBookingDetailsCard extends StatelessWidget {
  const CBookingDetailsCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}